---
sidebar_position: 3
sidebar_label: The BlockEntity Class
title: The BlockEntity Class
---

import AdditiveCodeHeader from "@site/src/components/AdditiveCodeHeader";
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

In addition to a `Block` class, we need a `BlockEntity` class to so that custom rendering can be performed for the block.

:::note
This tutorial is specifically for GeckoLib implementation. For information on general-modding block features, seek
a tutorial or community support.
:::

## Creating the class
<AdditiveCodeHeader />

First, we create the base `BlockEntity` class:

```java
public class ExampleBlockEntity extends BlockEntity {

}
```

Then, we will implement the `GeoBlockEntity` interface and override the `getAnimatableInstanceCache` and `registerControllers` methods:

```java
//highlight-next-line
public class ExampleBlockEntity extends BlockEntity implements GeoBlockEntity {
    //highlight-start
    @Override
    public void registerControllers(final AnimatableManager.ControllerRegistrar controllers) {
        // We can fill this in later
    }
    //highlight-end

    //highlight-start
    @Override
    public AnimatableInstanceCache getAnimatableInstanceCache() {
        return null;
    }
    //highlight-end
}
```

Next, let's instantiate an `AnimatableInstanceCache` in a `final` field at the top of our class, and return it in `getAnimatableInstanceCache`:

```java
public class ExampleBlockEntity extends BlockEntity implements GeoBlockEntity {
    //highlight-next-line
    private final AnimatableInstanceCache geoCache = GeckoLibUtil.createInstanceCache(this);

    @Override
    public void registerControllers(final AnimatableManager.ControllerRegistrar controllers) {
        // We can fill this in later
    }

    @Override
    public AnimatableInstanceCache getAnimatableInstanceCache() {
        //highlight-next-line
        return this.geoCache;
    }
}
```

<Tabs groupId="modloader" queryString="modloader">
    <TabItem value="fabric" label="Fabric">
        Then we add the constructor, referencing our `BlockEntity` type (which we will register later):

        ```java
        public class ExampleBlockEntity extends BlockEntity {
            private final AnimatableInstanceCache geoCache = GeckoLibUtil.createInstanceCache(this);

            //highlight-start
            public ExampleBlockEntity(BlockPos pos, BlockState state) {
                super(BlockEntityRegistry.EXAMPLE_BLOCK_ENTITY, pos, state);
            }
            //highlight-end

            @Override
            public void registerControllers(final AnimatableManager.ControllerRegistrar controllers) {
                // We can fill this in later
            }

            @Override
            public AnimatableInstanceCache getAnimatableInstanceCache() {
                return this.geoCache;
            }
        }
        ```
    </TabItem>
    <TabItem value="forge" label="Forge">
        Then we add the constructor, referencing our `BlockEntity` type (which we will register later):

        ```java
        public class ExampleBlockEntity extends BlockEntity {
            private final AnimatableInstanceCache geoCache = GeckoLibUtil.createInstanceCache(this);

            //highlight-start
            public ExampleBlockEntity(BlockPos pos, BlockState state) {
                super(BlockEntityRegistry.EXAMPLE_BLOCK_ENTITY.get(), pos, state);
            }
            //highlight-end

            @Override
            public void registerControllers(final AnimatableManager.ControllerRegistrar controllers) {
                // We can fill this in later
            }

            @Override
            public AnimatableInstanceCache getAnimatableInstanceCache() {
                return this.geoCache;
            }
        }
        ```
    </TabItem>
    <TabItem value="neoforge" label="NeoForge">
        Then we add the constructor, referencing our `BlockEntity` type (which we will register later):

        ```java
        public class ExampleBlockEntity extends BlockEntity {
            private final AnimatableInstanceCache geoCache = GeckoLibUtil.createInstanceCache(this);

            //highlight-start
            public ExampleBlockEntity(BlockPos pos, BlockState state) {
                super(BlockEntityRegistry.EXAMPLE_BLOCK_ENTITY.get(), pos, state);
            }
            //highlight-end

            @Override
            public void registerControllers(final AnimatableManager.ControllerRegistrar controllers) {
                // We can fill this in later
            }

            @Override
            public AnimatableInstanceCache getAnimatableInstanceCache() {
                return this.geoCache;
            }
        }
        ```
    </TabItem>
</Tabs>

And you're done!

For information on adding animation controllers, see the [Animation Controllers](/docs/Concepts/Animation/Controller/overview.mdx) page.