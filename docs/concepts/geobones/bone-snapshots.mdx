---
sidebar_position: 1
sidebar_label: Bone Snapshots
title: Bone Snapshots
---

import Collapsible from "@site/src/components/collapsible";

`BoneSnapshot`s are GeckoLib's method of storing bone transformations for animations.

## Overview
In the past, GeckoLib has allowed developers to directly modify `GeoBones` when animating. This has long been a source
of consistency and concurrency issues, and especially in recent times has resulted in a number of mostly unfixable bugs.

In GeckoLib5, `BoneSnapshots` were redesigned to act as the sole owner of all transformations a given bone has had applied
for a render pass.

## Ideology
The idea behind `BoneSnapshot`s is that when the [RenderPassInfo](/concepts/rendering/renderpassinfo.mdx) renders a model
for the first time in a render pass, it computes all the modifications for each bone, and stores them in `BoneSnapshot`s.

This means that the render pass can be re-run at any time, in any order, and as many times as needed, and animations only
need to be computed once.
Additionally, it means that the bone holds no record of its modifications; and can't possibly leak into other rendered animatables.

At the end of the render pass, all `BoneSnapshot`s are discarded entirely, leaving a clean slate.

## Usage
When the `GeoRenderer` begins a render pass, it calls <code>#adjustModelBonesForRender</code>.<br/>
Users wanting to animate bones manually for a render pass should override this.

```java
@Override
public void adjustModelBonesForRender(RenderPassInfo<R> renderPassInfo, BoneSnapshots snapshots) {

}
```

The `BoneSnapshots` object is provided to provide users access to the bones for animating.<br/>
See below for an example of animating a bone manually using `BoneSnapshots`.

<Collapsible title="Example Usage">
    Below, we animate the bone named "MyBoneName" by rotating it 45 degrees.
    ```java
    @Override
    public void adjustModelBonesForRender(RenderPassInfo<R> renderPassInfo, BoneSnapshots snapshots) {
        //highlight-start
        snapshots.ifPresent("MyBoneName", boneSnapshot -> {
            boneSnapshot.setRotX(45 * Mth.DEG_TO_RAD);
        });
        //highlight-end
    }
    ```
</Collapsible>

:::info
It is generally recommended to avoid using manual bone manipulation via `BoneSnapshots`, and instead incorporate it into
Molang variables and do it via JSON animations.
:::